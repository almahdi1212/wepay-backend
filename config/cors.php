<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    |
    | هذا الملف يتحكم في إعدادات CORS الخاصة بـ Laravel.
    | يسمح بالوصول من localhost أثناء التطوير ومن روابط النشر مثل Render أو Vercel.
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    // 🟡 السماح بكل أنواع الطلبات (GET, POST, PUT, DELETE...)
    'allowed_methods' => ['*'],

    // 🟢 السماح بهذه النطاقات فقط (بدون *)
    'allowed_origins' => [
        'http://localhost:5173',
        'http://127.0.0.1:5173',
        'https://wepay-frontend.vercel.app',
        'https://wepay-backend-y41w.onrender.com',
        '*', // احتياطي

    ],

    // 🟠 لا حاجة لتفعيل الأنماط
    'allowed_origins_patterns' => [],

    // 🟢 السماح بجميع الـ Headers من الواجهة
    'allowed_headers' => ['*'],

    // ⚪ لا حاجة لرؤوس إضافية
    'exposed_headers' => [],

    // ⚙️ مدة التخزين المؤقت (اختياري)
    'max_age' => 0,

    // 🚫 عادة false إلا إذا كنت تستخدم Cookies أو Sessions
    'supports_credentials' => false,
];
