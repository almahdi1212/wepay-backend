<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    |
    | إعدادات التحكم في CORS الخاصة بتطبيق Laravel
    | تسمح بطلبات من بيئة التطوير (localhost) ومن مواقع النشر (Vercel, Render)
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    // 🟡 السماح بكل أنواع الطلبات (GET, POST, PUT, DELETE...)
    'allowed_methods' => ['*'],

    // 🟢 السماح فقط بالنطاقات التالية
  'allowed_origins' => [
    'https://sheinlibya.com',
    'http://localhost:5173/',
    'https://www.sheinlibya.com',
],

    // 🟠 لا حاجة لأنماط regex هنا
    'allowed_origins_patterns' => [],

    // 🟢 السماح بجميع رؤوس الطلبات من الواجهة
    'allowed_headers' => ['*'],

    // ⚪ لا حاجة لإضافة رؤوس مكشوفة
    'exposed_headers' => [],

    // ⚙️ مدة التخزين المؤقت للاستجابة (اختياري)
    'max_age' => 0,

    // ⚪ اجعلها false إلا إذا كنت تستخدم Cookies/Sessions
    'supports_credentials' => false,
];
