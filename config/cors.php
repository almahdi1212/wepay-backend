<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    |
    | هذا الملف يتحكم في إعدادات السماح بالوصول بين النطاقات (CORS)
    | نسمح هنا لواجهة localhost (الفرونت أثناء التطوير)
    | وأيضًا لأي دومين آخر مثل Vercel أو Render
    |
    */

    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    // 🟡 السماح بكل أنواع الطلبات (GET, POST, PUT, DELETE, ...)
    'allowed_methods' => ['*'],

    // 🟢 السماح للنطاقات التالية بالاتصال
    'allowed_origins' => [
        'http://localhost:5173',           // ← أثناء التطوير المحلي
        'http://127.0.0.1:5173',           // ← بعض المتصفحات تستخدم هذا
        'https://wepay-frontend.vercel.app', // ← عندما ترفع الفرونت على Vercel
        'https://wepay-backend-y4w.onrender.com', // ← نفس الدومين في Render
        '*', // ← كإجراء احتياطي (يُستخدم فقط إذا لم يتم تحديد النطاق بدقة)
    ],

    // لا حاجة للأنماط هنا
    'allowed_origins_patterns' => [],

    // السماح بكل أنواع الرؤوس (Headers)
    'allowed_headers' => ['*'],

    // لا نعرض أي رؤوس إضافية في الاستجابة
    'exposed_headers' => [],

    // المدة الزمنية للتخزين المؤقت للطلب (اختياري)
    'max_age' => 0,

    // عادةً false إلا إذا كنت تستخدم cookies عبر النطاقات
    'supports_credentials' => false,
];
